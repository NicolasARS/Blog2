{% extends 'base.html.twig' %}
{% block title%}Admin Zone{% endblock %}
{% block body%}
<div class="container">
    <div class="col-xs-12 col-sm-8 col-sm-push-2">
       	<h1>Admin Zone</h1>
		<hr>
        <table class="table">
					<thead>
					<tr>
						<th scope="col">Sections</th>
						
					</tr>
					</thead>
					<tbody>
					<tr>
						<td>
							<button class="btn sr-button"><a href="{{path('app_categories')}}">Categories</a></button>
						</td>
						<td>
							<button class="btn sr-button"><a href="{{path('app_images')}}">Images</a></button>
						</td>
						<td>
							<button class="btn sr-button"><a href="{{path('add_images')}}">Add images</a></button>
						</td>
					</tr>
					</tbody>
				</table>
    </div>
</div>
{% endblock %}